const { Button } = require('./button.element');
const { Input } = require('./input.element');
const { Span } = require('./span.element');
const { Collection } = require('./collection.element');

module.exports = {
  Button,
  Input,
  Span,
  Collection
};
